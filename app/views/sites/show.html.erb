<script src="http://maps.google.com/maps/api/js?sensor=false">
</script>

<script type="text/javascript">


function displayPosition() {
    var mylat = <%= @site.latitud %>;
    var mylong = <%= @site.longitud %>;
    var latlng = new google.maps.LatLng(mylat, mylong);
    var myOptions = {
      zoom: 15,
      center: latlng,
      mapTypeId: google.maps.MapTypeId.HYBRID
    };
   
   var map = new google.maps.Map(document.getElementById("map_canvas"), myOptions);

//Add marker
   var marker = new google.maps.Marker({
	     position: latlng, 
	     map: map, 
	     title:"You are here"
	 });
}


</script>


<div id="site">
 
  <h1><%= @site.type.name if @site.type %></h1> 

<div id="map_canvas"></div>
<script type="text/javascript">
   displayPosition();
</script>


  <%= image_tag(@site.image.url, :class => 'site_image') %>

  <h3><%= @site.name %></h3> 

  <p><%=sanitize @site.description %></p>

  <p><b>Autor:</b> 
          <%= @site.user.name if @site.user %></p>

  <div class="visitas">
   <b>Visitas:</b> <%= @site.visitas %>
  </div

  <p><b><%= (link_to 'Comentarios >>', site_comentarios_path(@site.id)) if (@site.comentarios.size > 0) %></b></p>

  <p><b><%= link_to 'Añadir Comentario', new_comentario_path %>


</div>

<br/>

<p />
<% if @site.user == current_user %>
  <%= link_to 'Edit', edit_site_path(@site) %> |
<% end %>
<%= link_to 'Back', sites_path %>
