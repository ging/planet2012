<h1>Editing comentario</h1>


<%= form_for([@site, @comentario]) do |comentario| %>
  <% if @comentario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comentario.errors.count, "error") %> prohibited this site from being saved:</h2>

      <ul>
      <% @comentario.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= comentario.text_area :coment %>
  </div>
 
  <div class="actions">
    <%= comentario.submit  %>
  </div>
<% end %>

<%= link_to 'Back', sites_path %>
