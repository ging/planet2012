<h1>Editing comment</h1>

<% if @comment.user_id == current_user.id %>
<%= render 'form' %>
<% else %>
Permiso denegado.<br>
<% end %>

<%= link_to 'Show', @comment %> |
<% if @comment.site_id %>
<%= link_to 'Back', site_comments_path(@comment.site_id) %>
<% else %>
<%= link_to 'Back', "javascript:history.back()" %>
<% end %>

